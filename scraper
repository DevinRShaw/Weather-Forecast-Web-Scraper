#basic web scraping exercise 
from bs4 import BeautifulSoup
import requests





#First we need to request the HTML document from the website 
URL = 'http://books.toscrape.com/'
page = requests.get(URL)

#Then use beautiful soup to parse the HTML
soup = BeautifulSoup(page.content, "html.parser")

#Will make our html viewable in nice format
print(soup.prettify)

#creates a list of top level tags on the html
list(soup.children)
#prints the types of the tags we can access 
print([type(item) for item in list(soup.children)])